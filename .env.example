PORT=8080
AUTH_BEARER=change-me
OPENAI_API_KEY=sk-...
TIMEZONE=Australia/Melbourne
# Comma-separated list of RSS/Atom feeds (override defaults)
FEEDS=
# Minutes between polling runs
POLL_EVERY_MINUTES=30
# Cosine similarity threshold to consider articles same topic (0..1)
CLUSTER_SIM_THRESHOLD=0.82
